# 1. Введення даних від користувача
print("--- Програма для знаходження мінімуму з трьох чисел ---")

try:
    # Отримуємо три числа
    a = float(input("Введіть перше число (a): "))
    b = float(input("Введіть друге число (b): "))
    c = float(input("Введіть третє число (c): "))

except ValueError:
    print("\nПомилка: Ви ввели некоректні дані. Спробуйте ще раз, використовуючи лише числа.")
    # Зупиняємо виконання, якщо введення некоректне
    exit()

# 2. Логіка перевірки за допомогою if-elif-else
if a <= b and a <= c:
    # Якщо 'a' менше або дорівнює 'b' І менше або дорівнює 'c', то 'a' - мінімум
    minimum = a
elif b <= c:
    # Якщо попередня умова хибна (тобто 'a' не мінімум),
    # перевіряємо, чи 'b' менше або дорівнює 'c'. Якщо так, 'b' - мінімум
    minimum = b
else:
    # Якщо обидві попередні умови хибні, то 'c' - мінімум
    minimum = c

# 3. Виведення результату
print("\n--- Результат ---")
print(f"Введені числа: a={a}, b={b}, c={c}")
print(f"Найменше (мінімальне) число: **{minimum}**")
